# 作業ルール

## 判断が必要な箇所は実装せず、提案を書いて次へ進む

デザイン・UI・表示仕様について判断が必要になったら、その場で決めて実装しない。
`proposals/` に提案ファイルを1枚書き、**手を止めずに次のタスクへ進む**。

提案の採否を決めるのは人間の側。承認を待つために作業全体を止めない。

### 「判断が必要」とは

合理的な選択肢が複数あり、どれを選ぶかで**利用者の見え方が変わる**もの。

**提案を書く（実装しない）**

- CLI 出力の書式、項目の並び、ラベルの文言
- 表やレポートのレイアウト、区切り方、省略や丸めの仕方
- 記号・強調・色の使い分け
- README など読み手に向けたドキュメントの構成
- 利用者の目に触れる用語の選び方

**承認なしで進めてよい**

- データ層 — 取得・パース・保存・集計・検査。**テストが通れば進める**
- スキーマ、クエリ、閾値の実装
- バグ修正、リファクタリング、エラー処理
- 誤字や事実誤りの訂正

迷ったら提案を書く側に倒す。ただしデータ層の判断まで提案に回さない。
データ層で選択肢に迷った場合は、根拠をテストで示して進めるのが正しい。

### 進行が止まってしまう場合

表示を伴う機能で、何も出力しないと先へ進めないときに限り、最小の暫定実装を置いてよい。
その場合は提案ファイルに **暫定実装あり** と明記し、どのファイルのどこかを書く。
暫定実装は最も控えめな選択肢にする（既存書式の踏襲、装飾なし、情報の省略なし）。

### 書き方

`proposals/NNN-kebab-case-slug.md`。`NNN` は3桁の連番。

`proposals/TEMPLATE.md` を写して使う。次の4節は必須。

- `## 何に迷ったか`
- `## 選択肢`
- `## 推奨案とその理由`
- `## 保留による影響範囲`

書式は `python scripts/check_proposals.py` で検査される。
節が欠けていると落ちるため、提案は書き切ってから次へ進むこと。

### 提案を書いたあと

会話の最後に、書いた提案ファイルのパスと1行の要約を報告する。
埋もれると人間が判断できないため、静かに置いて終わらない。

## このリポジトリについて

Apple Marketing Tools RSS から日本の most-played 楽曲チャートを日次で記録する。
構成・データの持ち方・自動実行の仕様は `README.md` を参照。

- 標準ライブラリのみで動かす。依存を増やさない
- コメントと文書は日本語。理由（なぜそうしたか）を書き、動作の言い換えは書かない
- 過去日は取得し直せない。データの確保を検査より優先する

### ワークフローの意味を混ぜない

- `daily-chart` が赤い = **その日のデータが欠けている**。この意味に限定する。
  データ以外の理由でここを赤くしない
- コードのテストと提案の書式検査は `checks` に置く。ここが赤くてもデータは無事
- 検査を足すときは、どちらの意味に属するかを先に決める。迷ったら `checks` へ

`scripts/` の命名も揃える。`check_*.py` は検査、`test_*.py` はテストで、
テストは `python -m unittest discover -s scripts -p "test_*.py"` に自動で拾われる。
